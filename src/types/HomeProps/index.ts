import {PostType} from '@/types/PostType';

type Props = {
    result: PostType[],
    admit: PostType[],
    admissions: PostType[],
    answerkeys: PostType[],
    governments: PostType[],
    importants: PostType[],
    jobs: PostType[],
    syllabi: PostType[],
  }

export default Props;